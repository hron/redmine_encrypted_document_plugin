Dir.glob("#{File.dirname(__FILE__)}/tasks/*.rake").each {|f| load f}

namespace :redmine do
 namespace :encrypted_documents do
    desc "Initialize GnuPG home and generate private & public keys pair."
    task :init do
      puts RAILS_ROOT
    end
 end
end
